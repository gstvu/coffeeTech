🚀 CRUD de Usuários com Express e MySQLEste é um projeto de exemplo que demonstra a criação de um CRUD (Create, Read, Update, Delete) simples para gerenciamento de usuários, utilizando Node.js, o framework Express para as rotas da API, e o driver mysql2 para interação com um banco de dados MySQL.

🌟 FuncionalidadesO projeto oferece as seguintes rotas na API para manipulação de dados na tabela usuarios:Método HTTPRotaDescriçãoOperação CRUDGET/usuariosLista todos os usuários cadastrados.READPOST/usuariosCria um novo usuário. Requer nome e email no corpo (JSON).CREATEDELETE/usuarios/:idApaga um usuário com base no id fornecido na URL.DELETE🛠️ Configuração do ProjetoSiga os passos abaixo para configurar e rodar o projeto localmente.1. Pré-requisitosCertifique-se de ter instalado em sua máquina:Node.js (versão 18 ou superior, conforme package-lock.json).Um servidor MySQL em execução.2. Instalação de DependênciasO projeto utiliza as seguintes bibliotecas: express, mysql2, e dotenv.Bashnpm install
3. Configuração do Banco de DadosCriação da Tabela: Você precisará criar um banco de dados e uma tabela usuarios. O código assume que existe um banco de dados chamado projeto_node.SQL-- Exemplo de comando SQL para criar a tabela de usuários
CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE
);
Arquivo de Variáveis de Ambiente (.env):O arquivo .env contém as credenciais de conexão do MySQL e é lido pelo script db.js.⚠️ Atenção: Seus valores podem ser diferentes. O exemplo fornecido usa as credenciais: DB_HOST=localhost, DB_USER=root, DB_PASSWORD=senai, DB_NAME=projeto_node1.4. Inicialização do ServidorExecute o arquivo server.js para iniciar o servidor Express:Bashnode server.js

Se a conexão com o banco de dados for bem-sucedida, você verá as seguintes mensagens no console:🔌 MySQL Conectado com Sucesso!
🚀 Servidor rodando em http://localhost:3000

💻 Como TestarO servidor estará rodando na porta 3000. Você pode usar ferramentas como Postman, Insomnia ou o comando curl para testar as rotas da API.Exemplo de Requisições:OperaçãoMétodo / RotaExemplo de curlLISTARGET /usuarioscurl -X GET http://localhost:3000/usuariosCRIARPOST /usuarioscurl -X POST -H "Content-Type: application/json" -d '{"nome":"Maria", "email":"maria@exemplo.com"}' http://localhost:3000/usuariosDELETARDELETE /usuarios/1curl -X DELETE http://localhost:3000/usuarios/1 (substitua 1 pelo ID real)📄 Estrutura do CódigoO projeto está organizado nos seguintes arquivos:server.js: Contém o código principal do servidor Express, a configuração dos middlewares e a definição das rotas da API (CRUD).db.js: Módulo responsável por estabelecer e exportar a conexão com o banco de dados MySQL, utilizando variáveis de ambiente..env: Arquivo para armazenar as credenciais do banco de dados (ignoradas pelo Git em um projeto real).package.json / package-lock.json: Arquivos de metadados e dependências do Node.js.📝 ObservaçõesO arquivo server.js usa uma variável global (connection) para armazenar a conexão com o banco de dados e garante que o servidor só comece a escutar as requisições após a conexão ser estabelecida com sucesso, tratando erros fatais na conexão inicial.A rota app.use(express.static('public')); está configurada para servir arquivos estáticos de uma pasta public, caso haja uma interface frontend em HTML/CSS/JS.